        // $builder = $this->userModel->builder();
        // $builder->where('type', DEFAULT_USER_TYPE);

        // if ($deleted) {
        //     if ($deleted === 'no') {
        //         $builder->where('deleted_at', null);
        //     }

        //     if ($deleted === 'yes') {
        //         $builder->where('deleted_at !=', '');
        //     }
        // }

        // if ($email_verified) {
        //     if ($email_verified === 'yes') {
        //         $builder->where('email_verified', 1);
        //     }

        //     if ($email_verified === 'no') {
        //         $builder->where('email_verified', 0);
        //     }
        // }

        // $total_records = $builder->get()->resultID->num_rows;
        // $query = $builder->get($per_page, $page);

        // $users = $query->getResult();
        // $this->pager->makeLinks($page, $per_page, $total_records, 'bs_full');